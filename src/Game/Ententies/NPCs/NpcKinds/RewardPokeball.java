package Game.Ententies.NPCs.NpcKinds;

import Game.Ententies.NPCs.Npc;
import Game.Map;

import java.awt.*;

public class RewardPokeball extends Npc {
    public RewardPokeball(Map map, char glyph, Color color, int x, int y, String name) {
        super(map, glyph, color, x, y, name);
    }
}
